# 08: 锁餐 + 一键重生成

**What to build:** 用户可锁定任意单餐（图标标记），点击"一键重生成"时保留锁定餐次、重排其余，交互连贯不丢已选内容。

**Blocked by:** 07 (输入表单 + 结果展示 + 原理面板)

**Status:** ready-for-agent

- [ ] 单餐锁定切换 + 视觉标记
- [ ] 重生成请求携带 `locked`，结果中锁定餐次不变
- [ ] 提供"清空锁餐后重生成"入口
